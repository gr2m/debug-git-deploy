{
  "name": "debug-git-deploy",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "exit 0",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "repository": "github:gr2m/debug-git-deploy",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages-deploy": "^0.3.0",
    "semantic-release": "^4.3.5"
  },
  "gh-pages-deploy": {
    "noprompt": true,
    "staticpath": "demo"
  }
}
